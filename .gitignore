# Dependencies
node_modules

# Local secrets (use .env.example as the template)
.env

# Editor / tooling caches
.php-cs-fixer.cache

# Scratch / build output

# Database
# Track the seed dump in db/init/, but never commit local dump exports or volume backups
db/init/local-*.sql
db_data_backup.tar.gz