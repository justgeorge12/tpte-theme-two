# Copy this file to .env and fill in real values. .env is git-ignored.
#
# IMPORTANT: Docker Compose interpolates `$` in .env values. A literal `$` in a
# password must be escaped as `$$`, otherwise the part after `$` is silently
# dropped. Easiest path: use passwords WITHOUT `$`.

# DB
DB_USER=tpte_admin
DB_PASSWORD=sqlazzc321raxzchkfkdpk35ko!@
DB_ROOT_PASSWORD=szc51@agtgkoplmbvc34z
DB_PORT=3316
# WP
WP_PORT=4001
# phpMyAdmin
PMA_PORT=8081
WP_USER=tpteadmin
WP_PASS=ciK6CrBtlY8@ysblB
